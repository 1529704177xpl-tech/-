server:
  port: 8080

spring:
  application:
    name: JiSuGou
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/speed_mall?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: "000000"

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.xpl.jisugou.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl